// navigation
const hamburger = document.querySelector('.nav__hamburger')
const navMobileItems = document.querySelectorAll('.nav--mobile>.nav__item')
const menuMobile = document.querySelector('.nav--mobile')
const navItems = document.querySelectorAll('.nav--desktop>.nav__item')

//elements
const productBoxes = document.querySelectorAll('.product') //shop elements
const artistBoxes = document.querySelectorAll('.artists__artist') //bio elements
const albumBoxes = document.querySelectorAll('.album') //albums elements

// section
const intro = document.getElementById('intro')
const shop = document.getElementById('shop')
const calendar = document.getElementById('calendar')
const bio = document.getElementById('bio')
const albums = document.getElementById('albums')

// calendar
const calendarBox = document.querySelector('#calendar>.wrap')

//albums
const albumsBox = document.querySelector('#albums>.wrap')